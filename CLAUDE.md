# CLAUDE.md

## プロジェクト概要

このプロジェクトは、ブラウザで動作する営業ゲームアプリです。実装はシンプルな構成を保ち、ユーザーがブラウザで `index.html` を開くだけで確認できる状態を基本とします。

## ファイル構成

ソースコードは、次の3ファイルに分割して管理します。

- `index.html`: 画面の構造、表示要素、アクセシビリティに関するマークアップ
- `style.css`: レイアウト、色、タイポグラフィ、レスポンシブ対応、アニメーション
- `script.js`: ゲームの状態管理、ユーザー操作、表示更新、ゲームロジック

原則として、この3ファイル以外にフロントエンドの実装ファイルを追加しません。スタイルをHTMLへ直接記述したり、動作ロジックをHTMLへ直接記述したりせず、責務ごとに適切なファイルへ記述してください。

## 実装ルール

- 既存のコードや構成を尊重し、変更範囲は必要最小限にする。
- HTMLは意味のある要素を使い、キーボード操作や読み上げにも配慮する。
- CSSは再利用可能なクラスとCSSカスタムプロパティを活用し、インラインスタイルを避ける。
- JavaScriptはグローバル変数を増やさず、ゲーム状態と画面更新の責務を分ける。
- 外部ライブラリを導入する場合は、導入理由と読み込み方法を明確にする。
- 画面はデスクトップとモバイルの両方で崩れないようにする。
- 変更後はブラウザで主要な操作を確認し、コンソールエラーがないことを確認する。

## 秘匿情報の管理

- APIキー、アクセストークン、パスワード、接続文字列などの秘匿情報は `.env` ファイルで管理する。
- 秘匿情報をHTML、CSS、JavaScript、コミットメッセージ、ドキュメントへ直接記述しない。
- `.env`、`.env.*`、バックアップファイル、ログファイルなどの秘匿情報を含むファイルはGitHubへ絶対にプッシュしない。
- 共有が必要な環境変数は、実値を含まない `.env.example` に変数名とダミー値だけを記載する。
- `.env.example` にも実際の認証情報や本番環境の値を記載しない。
- コミット前に `git diff --cached` と `git status --short` を確認し、秘匿情報が含まれていないことを二重に確認する。
- 秘匿情報の漏えいが疑われる場合は、直ちに該当キーやパスワードを無効化・再発行し、GitHub上の履歴からの除去も行う。

## Git運用ルール

- コードを変更するたびに、変更内容を確認してコミットし、GitHubへプッシュする。
- 1つの変更目的ごとに、内容が分かるコミットメッセージを付ける。
- プッシュ前に変更差分を確認し、意図しないファイルや秘密情報が含まれていないことを確認する。
- 認証情報、APIキー、個人情報などの秘密情報はコミット・プッシュしない。
- プッシュ先のブランチやリモート設定が不明な場合は、推測で操作せず確認する。
- GitHubへのプッシュが失敗した場合は、失敗理由を確認して解消するまで完了扱いにしない。

## 作業手順

1. 変更対象と現在の状態を確認する。
2. 必要なファイルだけを変更する。
3. `index.html` をブラウザで開き、主要な表示と操作を確認する。
4. 開発者ツールでエラーがないことを確認する。
5. `git diff` で差分を確認する。
6. 変更をコミットする。
7. GitHubへプッシュする。
8. プッシュ結果を確認して作業完了とする。

## コミュニケーション

作業完了時は、変更したファイル、確認した内容、コミットID、GitHubへのプッシュ結果を簡潔に報告してください。
